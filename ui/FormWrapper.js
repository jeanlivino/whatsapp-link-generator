import { styled } from '~/stitches.config'

const FormWrapper = styled('div', {
  width: '100%',
  background: '#fff',
  borderRadius: '15px',
  padding: '30px',
  boxShadow: '2px 5px 34px 0px rgba(0, 0, 0, 0.33)',
})

export default FormWrapper
